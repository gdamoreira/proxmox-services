---
- name: Restart Docker
  ansible.builtin.systemd:
    name: docker
    state: restarted
    daemon_reload: true

- name: Restart Pihole
  community.docker.docker_compose_v2:
    project_src: "{{ pihole_data_dir }}"
    state: restarted
