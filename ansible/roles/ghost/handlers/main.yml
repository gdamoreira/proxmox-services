---
- name: Restart Docker
  ansible.builtin.systemd:
    name: docker
    state: restarted
    daemon_reload: true

- name: Restart Ghost
  community.docker.docker_compose_v2:
    project_src: "{{ ghost_data_dir }}"
    state: restarted
