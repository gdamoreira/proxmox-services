---
- name: Restart Docker
  ansible.builtin.systemd:
    name: docker
    state: restarted
    daemon_reload: true

- name: Restart GitLab
  community.docker.docker_compose_v2:
    project_src: "{{ gitlab_data_dir }}"
    state: restarted
