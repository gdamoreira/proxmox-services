---
- name: Include config vars
  ansible.builtin.include_vars: "vars/default.yml"

- name: Install NFS utils (Debian)
  ansible.builtin.package:
    name: nfs-common
    state: present
  when: backup_fstype == "nfs" and ansible_facts.os_family == "Debian"

- name: Install NFS utils (RedHat)
  ansible.builtin.package:
    name: nfs-utils
    state: present
  when: backup_fstype == "nfs" and ansible_facts.os_family == "RedHat"

- name: Install CIFS utils
  ansible.builtin.package:
    name: cifs-utils
    state: present
  when: backup_fstype == "cifs"

- name: Create Samba credentials directory
  ansible.builtin.file:
    path: /etc/samba/credentials
    state: directory
    mode: "0700"
  when: backup_fstype == "cifs"

- name: Deploy backup CIFS credentials
  ansible.builtin.copy:
    dest: "{{ backup_cifs_credentials_file }}"
    content: |
      username={{ backup_cifs_username }}
      password={{ backup_cifs_password }}
      domain=WORKGROUP
    mode: "0600"
  no_log: true
  when: backup_fstype == "cifs"

- name: Mount backup NFS share
  ansible.builtin.mount:
    src: "{{ backup_nfs_server }}:{{ backup_nfs_share }}"
    path: "{{ backup_mount }}"
    opts: "{{ backup_nfs_opts }}"
    fstype: nfs
    state: mounted
  when: backup_fstype == "nfs"

- name: Mount backup CIFS share
  ansible.builtin.mount:
    src: "{{ backup_cifs_server }}/{{ backup_cifs_share }}"
    path: "{{ backup_mount }}"
    opts: "{{ backup_cifs_opts }}"
    fstype: cifs
    state: mounted
  when: backup_fstype == "cifs"

- name: Deploy backup script
  ansible.builtin.template:
    src: "backup-{{ backup_service_name }}.sh.j2"
    dest: "/usr/local/bin/backup-{{ backup_service_name }}.sh"
    mode: "0700"

- name: Deploy systemd service
  ansible.builtin.template:
    src: backup.service.j2
    dest: "/etc/systemd/system/backup-{{ backup_service_name }}.service"
    mode: "0644"

- name: Deploy systemd timer
  ansible.builtin.template:
    src: backup.timer.j2
    dest: "/etc/systemd/system/backup-{{ backup_service_name }}.timer"
    mode: "0644"

- name: Enable and start backup timer
  ansible.builtin.systemd:
    name: "backup-{{ backup_service_name }}.timer"
    state: started
    enabled: yes
    daemon_reload: yes
