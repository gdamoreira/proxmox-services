[Unit]
Description=Weekly backup {{ backup_service_name }}
Requires=backup-{{ backup_service_name }}.service

[Timer]
OnCalendar={{ backup_timer_calendar }}
Persistent=true

[Install]
WantedBy=timers.target
